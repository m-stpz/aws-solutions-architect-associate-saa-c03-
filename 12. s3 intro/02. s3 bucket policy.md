## S3 Security

- User-based
  - IAM policies: which API calls should be allowed for a specific user from IAM

- Resource-based
  - bucket policies [most commonly used nowadays] - bucket wide rules from the S3 console
    - allows cross account
  - object access control list [acl] - finer grain [can be disabled]
  - bucket access control list [acl] - less common [can be disabled]

- Node: an IAM principal can access an S3 object if

1. The user IAM permissions ALLOW it OR the resource policy ALLOWS it
   - ALLOW
     - IAM permission OR
     - resource permission
2. AND there's no explict DENY
   - DENY
     - no explicit

- Encryption: encrypt objects in S3 using encryption keys

## Bucket Policies

- JSON based policies
  - Resource: buckets/objects
  - Effect: Allow/deny
  - Actions: set of services/API to allow/deny
  - Principal: the account or user to apply the policy to

```json
{
  "Version": "<>",
  "Statement": [
    {
      "Sid": "PublicRead",
      "Effect": "Allow",
      "Principal": "*",
      "Action": ["s3:GetObject"],
      "Resource": ["arn:aws:s3:::examplebucket/*"]
    }
  ]
}
```

- Use s3 bucket policy to:
  - grant public access to the bucket
  - force objects to be encrypted at upload
  - grant access to another account [cross account]

## Examples

1. Public access

- add s3 bucket policy that allows public access

2. IAM permissions

- Add IAM permissions to the user through a policy
- Then, he can access s3

3. EC2 instance

- Create an EC2 instance role
- Assign IAM permissions to the instance role
- Then the EC2 will be able to access the s3 bucket

4. Cross account access

- We have an IAM user in other AWS account
- We add to the s3 bucket policy: allow cross-account

## Hands on

- amazon s3 / buckets / <bucket-name>
- edit block public access and enable the public policy
  - which will turn off some ACLs checkboxes
- then, go to bucket policy
  - you also have the policy generator
