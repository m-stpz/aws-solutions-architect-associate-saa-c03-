# S3 - Replication

- It comes in two flavors, CRR and SRR
  - CRR = cross regional replication
    - use case: compliance, lower latency access, replication across accounts
  - SRR = same region replication
    - use case: log aggregation, live replication between prod and test accounts

- Must enabled versioning in source and destination buckets

> Replication ONLY works if versioning is ENABLED

- Buckets can be in different AWS accounts
- Copying is async
- Must give IAM permissions to S3

- After enabling replication, only new objects are replicated
  - to replicate existing objects, use S3 Batch replication
    - replicates existing objects and objects that failed replication

- For DELETE operations
  - can replicate delete markers from source to target [optional setting]
  - deletions with a version ID are not replicated [to avoid malicious deletes]

- There's no "chaining" of replication
  - if bucket 1 has replication into bucket 2, which has replication into bucket 3, then objects created in bucket 1 aren't replicated to bucket 3

## Hands on

- Amazon s3 / buckets / <bucket-origin>
- Management / replication rules
  - create replication rule
  - enable the rule
  - select the scope
    - using filters
    - all objects within bucket
  - select destination
    - in this account
    - in other account
  - select IAM role
  - in additional replication options/
    - delete market replication checkbox
    - by default, they are disabled, but you can enable them
  - replicate existing objects?

- With the replication, files you added on the origin will be propagated into the target
