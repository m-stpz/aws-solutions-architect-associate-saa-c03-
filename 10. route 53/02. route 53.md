# Route 53

- Highly available, scalable, fully managed and authoritative DNS
  - authoritative: the customer (you) can update the DNS records
- Route 53 is also a domain registrar
- Ability to check the health of your resources
- The only AWS service with 100% availability SLA
  - SLA = service level agreement
    - formal contract between a software/service provider and a customer that defines the expected level of service
- Why the name Route 53?
  - 53 is a reference to the traditional DNS port

- Route 53 performs 3 main functions:

1. domain registration
2. authoritative DNS resolution
3. health checking / traffic management

## Records

- How you want to route traffic for a domain
- Each record contains:
  - domain/subdomain name: e.g., example.com
  - record type: ex: A or AAAA
  - value: ex: 12.34.56.78
  - routing policy: how route 53 responds to queries
  - TTL: amount of time the record cached at DNS resolvers
    - "client, cache this result for the time of the TTL"
      - it's fine to have a long TTL given that we don't expect the domain to change
    - high: 24h
      - less traffic on route 53
      - possibly outdated records
    - low: 60s
      - increased traffic on route 53 ($$)
      - records at outdated for less time
      - easy to change records
    - except for alias records (CNAME), TTL is mandatory for each DNS record
- Route 53 supports the following DNS record types:
  - must know: A / AAAA / CNAME / NS
  - advanced: CAA / DS / MX / NAPTR / PTR / SOA / TXT / SPF / SRV

### Record types

- A: maps a hostname to ipv4
- AAAA: maps a hostname to ipv6
- CNAME: maps a hostname to another hostname
  - target is a domain name which must have an A or AAAA record
  - can't create a CNAME record for the top node of a DNS namespace (zone apex)
    - ex: you can't create an alias for example.com, but you can for www.example.com
- NS: name servers for the hosted zone
  - control how traffic is routed for a domain

### Hosted zones

- Container for records that define how to route traffic to a domain and its subdomains

- Public hosted zones: contains records that specify how to route traffic on the internet (public domain names)
  - `application1.mypublicdomain.com`
- Private hosted zones: contains records that specify how to route traffic within one or more VPCs (private domain names)
  - `application1.company.internal`

- Pay $0.5 per month per hosted zone

## Hands on

1. Registering a domain

- Register domains
  - $12 a year
- You can select the duration + also auto-renew
- Once it's registered, go to `hosted zones`

2. Creating a record

- Go to route 53 / hosted zones / <domain> / create record
- Then type
  - record name, record type, value, TTL, routing policy
    - the value is the IP of the instance

3. creating an ec2 instance to route

- create your instance (also in different regions)
  - ex:
    - us-east-1
    - eu-central-1
    - ap-southeast-1
- launch load balancer

## Route 53 vs. load balancer

- What's the relationship of Route 53 and load balancer?
- Route 53: global dns layer
  - global routing
  - latency / geolocation / failover
  - decides which IP address/ALB endpoint to return to the client browser
  - solves cross-region routing:
    - directs European users to ALB in frankfurt and american users to ALB in virginia
      - based on latency or geography
  - heavily cached
    - even if route 53's health check marks an IP unhealthy, clients will continue being directed there until thei local DNS cache (TTL) expires
  - no visibility on current traffic volume
- load balancer: regional traffic and instance-level layer
  - regional routing
  - operates inside a SINGLE region
  - if specific backend instance is down, LB instantly stops sending requests to it
    - milliseconds, zero downtime
  - visibility into current traffic

> Route 53 is the airport flight dispatcher: it looks at the world map, checks if airports (regions) are open, and directs your plan to land at the correct regional airport
> ELB is the ground taxi director at the airport: once the plan lands in that specific airport (region), it directs it to an open terminal gate (healthy EC2 instance)

## Route 53: Serverless, Containers, and EC2 instances

```
                                  [ User / Client ]
                                          │
                                          ▼
                      ┌───────────────────────────────────────┐
                      │            Amazon Route 53            │
                      └───────┬──────────────┬─────────────┬──┘
                              │              │             │
        ┌─────────────────────┘              │             └─────────────────────┐
        │ (Alias / CNAME)                    │ (Alias)                           │ (Alias / CNAME)
        ▼                                    ▼                                   ▼
┌──────────────────────────────┐ ┌───────────────────────┐ ┌─────────────────────────────┐
│       1. Serverless          │ │     2. Containers     │ │         3. EC2 Fleet        │
│                              │ │     (ECS / EKS)       │ │                             │
│ ┌──────────────────────────┐ │ │ ┌───────────────────┐ │ │ ┌─────────────────────────┐ │
│ │ API Gateway / CloudFront │ │ │ │ Application LB    │ │ │ │ Application / Network LB│ │
│ └────────────┬─────────────┘ │ │ └─────────┬─────────┘ │ │ └────────────┬────────────┘ │
│              ▼               │ │           ▼           │ │              ▼              │
│      [ AWS Lambda ]          │ │   [ ECS / Fargate ]   │ │     [ Auto-Scaling EC2 ]    │
│                              │ │   [ Dynamic Ports ]   │ │     [ Fixed or Auto-Scale ] │
└──────────────────────────────┘ └───────────────────────┘ └─────────────────────────────┘
```

### EC2 instances

- It depends whether you have a single standalone server or a freel

- Single standalone EC2 (simple/dev environments)
  - route 53 `A` record maps your domain directly to the EC2 Elastic IP (EIP)

- Production fleet: route 53 -> ALB / NLB -> Auto-scaling EC2
  - Route 53 alias record points to the LB
  - LB manages health checks, SSL offloading, distributes traffic across multiple instances and multiple AZs

### Serverless: lambda, S3, API gateway

- AWS lambda functions and S3 static sites don't have fixed public IP addresses

- Pattern A: route 53 -> API gateway custom domain -> AWS lambda
  - setup an API gateway custom domain name
  - in route 53, create alias record pointing to the API gateway
  - API gateway handles throttling, auth, and invokes the lambda backend

- Pattern B: route 53 -> CloudFront -> S3 / Lambda function URL
  - use an alias record pointing to a CloudFront distribution
  - CloudFront acts as the global edge CDN and routes calls to your S3 bucket / lambda function

- Pattern C: route 54 -> ALB -> Lambda
  - ALB can register lambda functions directly as targets in a target group

### Containers (ECS on EC2 / Fargate, EKS)

- Container tasks scale up/down rapidly, acquire dynamic ephemeral IP addresses, and often run on dynamic host port mappings

- Standard public pattern: route 54 -> ALB -> ECS / EKS
  - because container IPs and ports change constantly, route 53 never points directly to an individual container
  - route 53 uses an alias record pointing to an ALB
  - ALB automatically tracks dynamic port allocations (via ECS service registration) and routes traffic to healthy container tasks

- Internal service discovery: Private Route 53 / AWS Cloud Map
  - for internal microservices talking container-to-container inside a VPC, ECS integrates with AWS Cloud Map to automatically write and update SRV / A records
  - records are updated in route 53 privated hosted zone whenever tasks spins up or is terminated

## Domain registrar vs. DNS Service
