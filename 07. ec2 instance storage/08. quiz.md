# EC2 Data Management Quiz

1. You have just terminated an EC2 instance in us-east-1a, and its attached EBS volume is now available. Your teammate tries to attach it to an EC2 instance in us-east-1b but he can't. What is a possible cause for this?

- EBS volumes are AZ locked
  - EBS volumes are created for a given AZ

2. You have launched an EC2 instance with two EBS volumes, Root volume type and the other EBS volume type to store the data. A month later you are planning to terminate the EC2 instance. What's the default behavior that will happen to each EBS volume?

- The root, by default, is deleted and the EBS volume type won't
  - By default, the root volume type is deleted as its "Delete on termination" attribute checked by default
  - Any other EBS volume types won't, as its "Delete on termination" attribute is disabled by default

3. You can use an AMI in N.Virginia Region us-east-1 to launch an EC2 instance in any AWS Region.

- False
  - AMI are built for a specific AWS region
  - You can't launch an EC2 instance using the AMI from another region
  - But, you can copy the AMI to the target aws region, then use it

4. Which of the following EBS volume types can be used as boot volumes when you create EC2 instances?

- `gp2`, `gp3`, `io1`, `io2`
  - when creating EC2 instances, only SSD types, not HDD, for EBS volumes can boot volumes

5. What is EBS Multi-Attach?

- Attach the same EBS volume to multiple EC2 instances in the same AZ
  - Same EBS volume to multiple EC2s in the **SAME** AZ

6.  You would like to encrypt an unencrypted EBS volume attached to your EC2 instance. What should you do?
    1. create EBS snapshot of EBS volume
    2. Copy the snapshot and tick the option to encrypt copied snapshot
    3. Use the encrypted snapshot to create a new EBS volume

```
EBS volume (unencrypted) -> EBS Snapshot
                                |
                 copy + tick option to encrypt copied snapshot
                                |
                                v
                        EBS Snapshot -> EBS volume (encrypted)
```

7. You have a fleet of EC2 instances distributes across AZs that process a large data set. What do you recommend to make the same data to be accessible as an NFS drive to all of your EC2 instances?

- EFS
  - EFS is a NFS (network file system)
  - allows to mount the same file system on EC2 instances that are in different AZs

```
[ Availability Zone 1 ]            [ Availability Zone 2 ]
┌───────────────────────────┐      ┌───────────────────────────┐
│  EC2 Instance A           │      │  EC2 Instance B           │
│        │                  │      │        │                  │
│        ▼                  │      │        x (Cannot connect) │
│  ( EBS Volume )           │      │                           │
└───────────────────────────┘      └───────────────────────────┘
```

```
                    ╔═════════════════════════════╗
                    ║       Amazon EFS            ║
                    ║ (Replicated across Region)  ║
                    ╚═════════════════════════════╝
                               │       │
             ┌─────────────────┘       └─────────────────┐
             ▼                                           ▼
  [ Availability Zone 1 ]                     [ Availability Zone 2 ]
┌───────────────────────────┐               ┌───────────────────────────┐
│  EC2 Instance A           │               │  EC2 Instance B           │
└───────────────────────────┘               └───────────────────────────┘
```

8. You would like to have a high-performance local cache for your application hosted on an EC2 instance. You don't mind losing the cache upon the termination of your EC2 instance. Which storage mechanism do you recommend as a Solutions Architect?

- Instance store
  - best disk I/O performance

9. You are running a high-performance database that requires an IOPS of 310,000 for its underlying storage. What do you recommend?

- This was tricky. There were 4 options: gp2, io1, io2, ec2 instance store
  - I choose [in order]: io2 -> gp2 -> io1 -> ec2 instance store [last, and correct]
  - but why?
    - initially, I thought that it wouldn't be the case because ec2 instance store is ephemeral (I was correct on that), however...

- ... You can run a db on a EC2 instance that uses an instance store:
  - however, the problem is that data is lost if the EC2 instance is stopped
  - solution 1: set up a replication mechanism on another EC2 instance with an instance store to have a standby copy
  - solution 2: set up backup mechanism for the data

... In this case, due to the extremely high IOPS (310k), we'd need to use this (unconventional) solution
